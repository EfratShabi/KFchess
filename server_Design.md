מסמך עיצוב ארכיטקטוני — שרת Kung Fu Chess בקנה מידה רחב
1. יעדי מערכת
100M משתמשים רשומים
10M משתמשים פעילים בו-זמנית (CCU)
5M בקשות/שנייה (~1GB/s רוחב פס)

ארכיטקטורה: מיקרו-שירותים מבוזרים, קונטיינרים (Docker), תזמור (Kubernetes).

2. רכיבי הליבה

Load Balancer — שער כניסה יחיד ל-HTTP/WebSocket, מפזר תעבורה בין שרתי אימות ושידוך.

Matchmaking — Stateless, מבוזר

מאות instances חסרי-מצב, auto-scaling.
לא מחזיקים תור בזיכרון מקומי — פונים ל-Redis Cluster עם Sorted Sets (ZSET) לשליפה/שידוך מהיר.
Sharding: מפתחות תור לפי דירוג (queue:1200-1400) מתפזרים בין צמתי Redis באמצעות Hash Slots.

Game Servers — Stateful, בעלות יחידה על חדר

כל חדר משויך למופע שרת ספציפי ויחיד (מודל חובה, לא ברירת מחדל — ראו סעיף 6).
מנוהל ב-RAM: קונטיינר אחד מארח אלפי משחקים פעילים.
Game Tick Loop קבוע ב-100ms לחישובי תנועה/cooldown/collision. מהלך בפועל לא ממתין לטיק — מטופל ומאושר מיד עם קבלתו; הטיק משמש רק לחישובים רציפים.
Latency Symmetry: שידוך למשחק בשרת גאוגרפי-אמצעי בין שני השחקנים (למשל שרת אירופי לזוג ישראל–ניו יורק).
3. שכבות נתונים
	קבוע (Persistent)	זמני (Transient)
טכנולוגיה	PostgreSQL / MySQL	Redis Cluster
תוכן	פרופילים, hash סיסמאות, היסטוריית Elo	מצב משחק חי, לוג מהלכים, מיפוי בעלי-חדרים
מדיום	דיסק/SSD	RAM

אבטחה:

מניעת SQL Injection: שאילתות מפרמטריות בשכבת ה-Repository בלבד.
אימות WebSocket: JWT חתום (user_id, room_id) מאומת ב-handshake; טוקן לא תקין → ניתוק מיידי. מונע ניחוש כתובות חדרים.
4. מחזור חיים של משחק
שידוך — Matchmaker מתאים לפי Elo, מייצר room_id, קובע Game Server A.
Handoff — תשובת HTTP חד-פעמית עם JWT + כתובת ws://game-server-a/play/123. הלקוח מנתק HTTP ופותח WebSocket ישיר לשרת A.
ניהול המשחק — שרת A הוא Source of Truth הבלעדי: מעבד פקודות ב-RAM ברמת מיקרו-שנייה, בודק cooldowns, מכריע סדר. כל אירוע (תחילת תנועה/נחיתה/קפיצה/תפיסה) נשלח מיידית דרך Observer מקומי — לא ממתין לטיק. בנוסף, כל 10 טיקים (=1 שנייה) נשלח Snapshot מלא — לא ערוץ העדכון הראשי אלא רשת ביטחון לסנכרון וזיהוי נתק. הלקוח מבצע אינטרפולציה מקומית בין עדכונים. אין Redis Pub/Sub בלולאת המשחק הפעילה.
גיבוי אסינכרוני (Eventual Consistency) — במקביל לשידור, שרת A כותב ל-Redis:
game:room_123:stream — לוג מהלכים ליניארי (ל-Replay).
game:room_123:meta — מצב לוח קנוני אחרון (Hash).
כתיבה היא Non-blocking / Fire-and-Forget: השרת דוחף ל-socket buffer וממשיך לטיק הבא בלי לחכות לרדיס.
סגירת חדר — תוצאה נשמרת ל-PostgreSQL, TTL קצר על מפתחות רדיס הזמניים, חיבורי השחקנים נסגרים.
5. Fault Tolerance & Failover
┌──────────────────────┐    ┌────────────────────────┐    ┌──────────────────────┐
│ 1. זיהוי נתק/כשל     │───►│ 2. Session Recovery API│───►│ 3. הקצאת שרת B      │
│ (RST או Heartbeat)   │    │                        │    │ ושחזור מהלכים ב-RAM │
└──────────────────────┘    └────────────────────────┘    └──────────────────────┘
זיהוי כשל
קריסת תהליך (OOM וכו') → OS שולח TCP RST מיידי, הלקוח מזהה תוך מילישניות.
נפילת מכונה שלמה → אין RST, החיבור קופא. הלקוח משתמש ב-Passive Timeout: 3–5 שניות ללא Snapshot (=3-5 Snapshots שהוחמצו) → מכריז מת. הסף חייב להיגזר מ-TICK_MS × SNAPSHOT_EVERY_N_TICKS בפועל, לא מספר שרירותי.
פנייה להתאוששות — הלקוח שולח POST ל-Session Recovery Service עם {room_id, last_seen_move} + JWT.
מניעת Split-Brain (Fencing Token) — השירות בודק ברדיס מי הבעלים הרשום מול ה-Registry התשתיתי (Kubernetes Liveness). אם יש סתירה (רדיס אומר A, K8s אומר A מת) — מריץ Lua Script אטומי שמעדכן room_123:owner = Server_B ומעלה Epoch/Version, ללא חשש מ-Race Condition. אם A מתעורר מ"גיהוק" רשת וינסה לכתוב, ה-Epoch המעודכן ידחה אותו והוא יכבה את עצמו. אם A בריא — מוחזרת כתובתו המקורית (מונע מעברים מיותרים).
שחזור מצב — שרת B ניגש ל-Redis Stream (לוג מהלכים) ול-Redis Hash (מצב אחרון), מריץ את רצף המהלכים על לוח וירטואלי נקי, שולח ROOM_RESUMED.
UX — הודעת "מנסה להתחבר מחדש..." ל-1.5–2 שניות; המשחק ממשיך בדיוק מהנקודה שנעצר.
6. פשרות הנדסיות מרכזיות

Stateful (RAM מקומי) לעומת Stateless (Redis בכל תור)

שנבחר: Stateful. יתרון — אפס פניות רשת לאימות מהלך, קריטי לקצב Kung Fu Chess. חיסרון — התאוששות מורכבת (סעיף 5), ומונע scale-down שרירותי כשיש משחקים פעילים.
"ה-Chess Exception": בשחמט תורות קלאסי, Stateless כמעט חינם (delta בין timestamps, בלי לולאת טיק חיה) כי בני אדם חושבים שניות ארוכות. Kung Fu Chess מבטל את זה לגמרי — cooldowns והתנגשויות בזמן אמת מחייבים Stateful, אחרת Redis קורס תחת מיליוני פניות/שנייה.

Heartbeats וזיהוי ניתוק

כיוון שרת→לקוח: מכוסה חינם ע"י ה-Snapshot התקופתי (נשלח גם ללא פעילות) — משמש כסימן חיים פסיבי, בלי הודעת Heartbeat ייעודית.
כיוון לקוח→שרת: לא מכוסה ע"י Snapshot. לקוח ששקע לא שולח כלום, ו-TCP לבדו עלול לקחת דקות לזהות. לכן הלקוח שולח Ping מיניאטורי כל שנייה, בלי תלות בפעילות.
קנה מידה: 10M פעימות לב/שנייה → לולאות אירועים אסינכרוניות (epoll/io_uring) + Min-Heap פנימי לתזמון timeouts כ-Scheduled Tasks, לא כבדיקות אקטיביות — ממזער תקורת CPU.