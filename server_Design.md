הנה מסמך העיצוב הארכיטקטוני הסופי והמעודכן בעברית בפורמט Markdown (MD).

כל המסקנות, הדיוקים ההנדסיים והתיקונים שנובעים מאופיו הייחודי של המשחק שלך (קונג פו שחמט - Kung Fu Chess) הוטמעו באופן הרמטי בתוך הטקסט: מודל ה-Stateful המחייב, הזרמת ה-Snapshots בקצב של 10 מילישניות המשמשים כסימני חיים פסיביים, מנגנון ה-Eventual Consistency המבוזר, שימוש ב-Lua Script אטומי ברדיס למניעת Race Conditions, וניקוי שגיאות ה-Markdown הקודמות.

מסמך עיצוב ארכיטקטוני: שרת שחמט בקנה מידה רחב (גרסת Kung Fu Chess סופית ומדויקת)
1. מבוא ויעדי מערכת
מסמך זה מתאר את הארכיטקטורה המשודרגת עבור שרת משחק השחמט, המתוכנן לתמוך בביצועים הבאים:

100 מיליון משתמשים רשומים.

10 מיליון משתמשים פעילים בו-זמנית (CCU).

5 מיליון בקשות בשנייה (רוחב פס רשת של כ-1 גיגה-בייט בשנייה).

כדי לעמוד בדרישות עומס אלו, המערכת מבוססת על ארכיטקטורת ענן מבוזרת של מיקרו-שירותים (Microservices) הפרוסים בתוך קונטיינרים קלים של Docker ומנוהלים על ידי מנהל תזמור (כמו Kubernetes).

2. פירוט רכיבי המערכת וארכיטקטורת האשכול (Cluster)
נתב עומסים (Load Balancer)
שער הכניסה הראשי של המערכת. הוא קולט את כל תעבורת ה-HTTP/WebSockets הנכנסת מהאינטרנט ומפיץ אותה בצורה דינמית בין שרתי האימות והשידוכים כדי למנוע עומס על נקודת כשל יחידה.

שירותי שידוך מבוזרים ללא מצב (Distributed Stateless Matchmaking)
כדי לטפס למסה האדירה של בקשות שידוך תחת עומס של 10M CCU, רכיב ה-Matchmaker הוא Stateless לחלוטין ומריץ מאות מופעים המשונים בגודלם אוטומטית (Auto-scaling).

ביזור מבוסס לוח מרכזי (Stateless): המופעים אינם מחזיקים רשימות המתנה מקומיות בזיכרון שלהם. במקום זאת, הם מתפקדים כפקידי רישום עצמאיים המנווטים שחקנים מול Redis Cluster מבוזר ומנצלים מבני נתונים מהירים (כמו Sorted Sets / ZSET) כדי לשלוף ולשדך שחקנים בצורה יעילה.

ביזור הנתונים (Sharding): ה-Redis Cluster אינו קופסה מרכזית אחת אלא אשכול מחשבים. הוא משתמש בחלוקה ארכיטקטונית שבה מפתחות התורים (למשל, תור לפי רמות queue:1200-1400) מתפזרים בצורה דינמית בין צמתי בסיס הנתונים השונים באמצעות Hash Slots סטנדרטיים של רדיס.

שרתי משחק בעלי מצב (Stateful Game Servers - מודל בעלות יחידה על חדר)
מכיוון שהמערכת משרתת משחק קונג פו שחמט (Kung Fu Chess) – משחק מהיר בזמן אמת (Real-time) נטול תורות שבו כולם מזיזים את כל הכלים במקביל – המערכת מחייבת מודל Stateful קשיח. כל משחק משויך למופע שרת משחק בעל מצב אחד וספציפי (למשל, Game Server A).

ניהול הלוח ב-RAM: מכונה פיזית אחת מארחת קונטיינרים מבודדים, כאשר כל אחד מהם מנהל אלפי משחקים פעילים במקביל בתוך זיכרון ה-RAM המקומי שלו. השרת מריץ לולאת "טיקים" חיה (Game Tick Loop) בקצב מהיר כדי לחשב בכל מאיות השנייה את מיקומי הכלים, זמני הצינון (Cooldowns) וחישובי התנגשויות (Collisions) בזמן אמת.

סימטריית השהיית רשת (Latency Symmetry): שכבת השידוכים מקצה למשחק שרת שנמצא גאוגרפית באמצע הדרך בין השחקנים (למשל, שרת באירופה עבור משחק בין שחקן מישראל לשחקן מניו יורק) כדי לאזן את זמני הפינג (Ping).

3. שכבות ניהול נתונים וזיכרון
אנו אוכפים הפרדה קשיחה בין נתוני ריצה זמניים ונדיפים לבין נתונים קבועים והיסטוריים:

מאפיין	שכבת נתונים קבועה (Persistent)	שכבת נתונים זמנית (Transient)
טכנולוגיה	PostgreSQL / MySQL	Redis Cluster (In-Memory DB)
נתונים מאוחסנים	פרופילי משתמשים, גיבובי סיסמאות, היסטוריית דירוגי Elo	גיבוי מצב משחק פעיל, היסטוריית מהלכים חיה, מפות ניתוב בעלי החדרים
מדיום אחסון	דיסק קשיח / SSD (קבוע)	זיכרון RAM (מהיר במיוחד, נדיף)
אבטחה, אימות שער וכניסה (Verification)
מניעת הזרקת SQL: הגנה קשיחה מפני התקפות קלט מנוהלת באופן טבעי בשכבת המאגר (Repository) באמצעות שאילתות מפרמטריות (Parameterized Queries) עם מיקומים שמורים (?). קלטי משתמש מטופלים אך ורק כמשתני נתונים ולעולם אינם מוערכים כקוד SQL להרצה. החזרת שגיאות כלליות לקליינט משמשת רק כשכבת עירפול משנית.

אימות טוקן ב-WebSocket: כדי למנוע מגורמים זדוניים לנחש כתובות של חדרים ולהתחבר ישירות לשרתי המשחק, שרת המשחק משמש ככרטיס כניסה מאובטח (ברקוד). בזמן לחיצת היד (Handshake) הראשונית של ה-WebSocket, הוא מאמת באופן מפורש טוקן JWT חתום דיגיטלית המכיל הצהרות קשיחות (user_id, room_id). חיבורים עם טוקן לא תקין נזרקים מייד.

4. מחזור חיים מלא של משחק וזרימה בזמן אמת
שלב 1: שידוך והקצאת חדר
נתב העומסים מנתב את יוסי (ישראל) ודני (ניו יורק) למופע Matchmaker פנוי. שירות השידוכים מתאים ביניהם לפי ה-Elo, מייצר מזהה חדר (room_123), וקובע שהמשחק ירוץ ב-Game Server A.

שלב 2: ניתוב מחדש של החיבור (Handoff)
ה-Matchmaker מחזיר לשני הרכיבים במחשב של השחקנים תשובת HTTP זמנית המכילה את טוקן ה-JWT המאובטח ואת כתובת ה-URL המדויקת של שרת המשחק: ws://[game-server-a.chess.com/play/123](https://game-server-a.chess.com/play/123). האפליקציות אצל השחקנים מנתקות את קשר ה-HTTP, ופותחות חיבור WebSocket ישיר וקבוע מול שרת A.

שלב 3: ניהול המשחק והסמכות (Source of Truth)
שרת משחק A מחזיק כעת את שני חיבורי ה-WebSocket אצלו בזיכרון ה-RAM ומשמש כסמכות הבלעדית לניהול המצב.

יוסי ודני מבצעים פעולות בזמן אמת 
→
 הבקשות נשלחות ב-WebSocket ישירות לשרת A.

המעבד של שרת A מעבד את הפקודות ב-RAM המקומי שלו ברמת המיקרו-שנייה, מכריע מי הגיע קודם, בודק Cooldowns ומעדכן את המצב.

שרת A מזרים באופן שוטף ותכוף תמונת מצב מלאה (Snapshot) של הלוח בכל 10 מילישניות (100 פעמים בשנייה) לשני השחקנים דרך ערוצי ה-WebSocket הפתוחים שלהם. בשלב זה של לולאת המשחק הפעילה, לא נעשה שימוש ב-Redis Pub/Sub.

שלב 4: גיבוי אסינכרוני מבוזר (Eventual Consistency)
במקביל להזרמת המשחק בטירוף, שרת A מסנכרן את המידע ומבצע כתיבה לשני מבני נתונים נפרדים ברדיס במקביל:

רשימה ליניארית מסודרת ב-Redis Stream (תחת המפתח game:room_123:stream) השומרת את לוג כל המהלכים לצורך Replay בעתיד.

מבנה Redis Hash (תחת המפתח game:room_123:meta) השומר את נתוני הלוח האחרונים הקנוניים.

הלוגיקה הריצתית (Non-blocking I/O): שרת A לא מבצע פנייה חוסמת וממתין לרדיס (מה שהיה גורם ללאגים והרס את חוויית ה-Real-time). הוא משתמש בקלט/פלט לא חוסם בצד שלו: הוא דוחף את הודעת הגיבוי לצינור הרשת (Socket Buffer) של מערכת ההפעלה, וממשיך בשיטת Fire-and-Forget מקומית ומיידית לטיק הבא של ה-Kung Fu Chess. רדיס המבוזר מעבד את ההודעות בצורה הסינכרונית והאטומית שלו ברקע. שאר חלקי המערכת (כמו שרת הצופים) מתעדכנים בקצב שלהם ומגיעים לעקביות בסופו של דבר (Eventual Consistency).

שלב 5: סגירת החדר
ברגע שהמשחק מסתיים, שרת A שומר את תוצאת המשחק הסופית לבסיס הנתונים הקבוע PostgreSQL, מגדיר זמן מחיקה קצר (TTL/Expire) על המפתחות הזמניים ברדיס כדי לפנות RAM, וסוגר את חיבורי השחקנים.

5. ארכיטקטורת התאוששות מכשל (Fault Tolerance & Live Failover)
אם שרת משחק (למשל, Game Server A) חווה כשל, פרוטוקול ההתאוששות מופעל בשני מסלולים נפרדים בהתאם לאופי התקלה, תוך הקרבה זמנית של ה-Availability למשך שניות בודדות לטובת שמירה קשיחה על ה-Consistency:

Plaintext
 ┌──────────────────────┐        ┌────────────────────────┐        ┌──────────────────────┐
 │  1. זיהוי הנתק/כשל   │        │ 2. פנייה יזומה ל-API   │        │ 3. הקצאת שרת B       │
 │  (RST או Heartbeat)  ├───────►│(Session Recovery Service)├───────►│ושחזור המהלכים ב-RAM  │
 └──────────────────────┘        └────────────────────────┘        └──────────────────────┘
פרוטוקול ההתאוששות (The Recovery Protocol):
1. זיהוי הכשל (Detection)
מסלול א' - קריסת תהליך מסודרת (Process Crash): אם התהליך של שרת A קורס עקב שגיאת זיכרון (OOM), מערכת ההפעלה של המכונה עדיין חיה ושולחת פקודת TCP RST (Reset) מיידית. הקליינטים קולטים את הנתק הפיזי בתוך מילישניות ומפעילים קוד התאוששות אוטומטי.

מסלול ב' - נפילת שרת מלאה או ניתוק רשת (Node Death): אם כל המכונה הפיזית נופלת, אין מי שישלח פקודת RST והחיבור קופא. במקרה זה, הזרמת ה-Snapshots של ה-10 מילישניות נעצרת. הקליינטים משתמשים בטקטיקה של Passive Timeout (פג תוקף סביל): אם עברו 500 מילישניות (חצי שנייה) רצופות של שקט מוחלט ללא Snapshot, הקליינט מכריז על השרת כמת ומאתחל את פרוטוקול ההתאוששות.

2. פנייה לשירות ההתאוששות
האפליקציה של השחקן שולחת בקשת HTTP POST לשירות ייעודי בשער שנקרא Session Recovery Service. הבקשה מציגה את הקשר החדר המקורי שלהם: { "room_id": "room_123", "last_seen_move": 14 } יחד עם ה-JWT.

3. הבוררות ומניעת "פיצול מוח" (Zombie Prevention / Split-Brain)
שירות ההתאוששות פונה לנתב הסטטוס של רדיס ובודק איפה חדר room_123 רשום. אם רדיס מראה ששרת A הוא הבעלים, אך ה-Registry התשתיתי (כמו Kubernetes Liveness Probes) מאותת ששרת A מת – שירות ההתאוששות בוחר שרת חדש ופנוי: Game Server B.

מנגנון ה-Fencing Token (מניעת שרת זומבי): שירות ההתאוששות משתמש ב-Lua Script אטומי ברדיס כדי לשנות את הערך ל-room_123:owner = Server_B ולהעלות את מספר הגרסה הרץ (Epoch/Version). ה-Lua Script מבוצע בתוך הזיכרון של רדיס כפרוסס אחד נעול, ללא יכולת של שרטים אחרים להשתחל באמצע (Race Condition). אם שרת A לא באמת מת אלא רק חווה "גיהוק" רשת פתאומי והתעורר, בפעם הבאה שהוא ינסה לבצע כתיבה אסינכרונית לרדיס, ה-Lua Script שיריץ יגלה שספר הגרסה עלה, וידחה את הכתיבה של שרת A באופן פעיל. שרת A יבין שהוא הודח ויסגור את עצמו.

הערת גיבוי: אם הבדיקה מגלה ששרת A בריא לחלוטין והניתוק היה רק אצל המשתמש, המערכת תחזיר את הכתובת של שרת A המקורי כדי למנוע מעברים מיותרים.

4. שחזור המצב ובניית הלוח (State Replay)
ברגע שיוסי ודני מקבלים את הכתובת החדשה, הם פותחים WebSocket מול שרת B.

שרת B קורא את מזהה החדר, ניגש ל-Redis Stream ושולף את "קלטת הווידאו" של היסטוריית המהלכים הליניארית, ובמקביל שולף את פרמטרי הלוח העדכניים מתוך ה-Redis Hash.

בשבריר מילישנייה, שרת B מריץ את רצף המהלכים על לוח שחמט וירטואלי נקי אצלו ב-RAM, משחזר את תמונת המצב המדויקת ששרת A הספיק לגבות, ומחזיר לשחקנים הודעת סינכרון: ROOM_RESUMED.

5. תוצאת חוויית משתמש (UX)
השחקנים רואים על המסך הודעה זמנית של "מנסה להתחבר מחדש..." למשך 1.5–2 שניות, והמשחק חוזר לחיים בדיוק מהנקודה שבה נפסק, מוגן מפני רמאויות או כשלים קטסטרופליים.

6. פשרות ארכיטקטוניות ודילמות הנדסיות (Trade-offs)
דילמה 1: ניהול זיכרון בעל מצב (Stateful) מול עיבוד ללא מצב (Stateless)
אפשרות א' (מצב מקומי ב-RAM - הדרך שנבחרה): שרת המשחק מחזיק את חוקי המשחק והמצב הקנוני ב-RAM המקומי של הקונטיינר.

יתרונות: מהירות ביצוע מקסימלית ואפס פניות לרשת או לבסיסי נתונים כדי לאמת חוקיות של מהלך – קריטי ותנאי מחייב לקצב הדינמי והמהיר של Kung Fu Chess.

חסרונות: דורש מנגנון התאוששות מורכב (כפי שמתואר בסעיף 5) ומונע אפשרות לכבות או לצמצם שרתים (Scale-down) באופן שרירותי בזמן שיש עליהם משחקים פעילים.

אפשרות ב' (עיבוד Stateless מול זיכרון מטמון מבוזר): שרת המשחק לא שומר כלום מקומית; בכל תור הוא מושך את היסטוריית המשחק מרדיס, בודק חוקיות, ודוחף חזרה לרדיס.

הערת הבהרה לגבי ה-"Chess Exception": במקור, משחקי שחמט מסורתיים מבוססי תורות מהווים מקרה קצה נוח שבו ה"חיסרון" של אפשרות ב' (פניית רשת לרדיס) כמעט זניח, כיוון שבני אדם חושבים שניות ארוכות והשעון מחושב בצורה פסיבית לפי דלתא של זמנים (remaining_time ו-last_move_timestamp) ללא צורך בלולאת שעון חיה. אולם, החרגה זו מבוטלת לחלוטין עבור Kung Fu Chess – אופי המשחק בזמן אמת, ריצת חישובי הצינון וההתנגשויות מחייבים מודל Stateful (אפשרות א') על מנת למנוע קריסה של רדיס תחת מיליוני פניות בשנייה.

דילמה 2: ניהול פעימות לב (Heartbeats) וזיהוי ניתוקים בצד הלקוח
כדי להפעיל ספירה לאחור ולהעניק הפסד טכני לשחקנים שנטשו את המשחק, הפלטפורמה חייבת להבדיל בין שחקן שממתין להזדמנות תקיפה לבין שחקן שהאינטרנט שלו התנתק.

הזרמה חד-כיוונית והפחתת תקורה (Overhead): השרת אינו נדרש לשלוח הודעות Heartbeat ייעודיות לשחקנים, מכיוון שהזרמת ה-Snapshots התכופה בכל 10 מילישניות משמשת כסימן חיים פסיבי מושלם. עם זאת, בכיוון ההפוך (מהלקוח לשרת), השחקן שולח הודעת Ping מיניאטורית פעם בשנייה כדי לאותת שהוא נוכח.

האופטימיזציה לעומס: כדי לנהל 10 מיליון פעימות לב נכנסות בשנייה מבלי לחנוק את המעבד (CPU Thread Exhaustion), שרתי המשחק משתמשים בלולאות אירועים אסינכרוניות (Asynchronous I/O Event Loops) מתקדמות (כמו epoll / io_uring) בשילוב עם מנגנוני תזמון מבוססי מבני נתונים מסוג Min-Heaps פנימיים (In-Memory) בתוך זיכרון התהליך. מבנה זה מבטיח שטיימרים של ניתוקים מנוהלים כמשימות מתוזמנות יעילות (Scheduled Tasks) ולא כבדיקות אקטיביות ויקרות, מה שממזער את תקורה של המעבד בקנה מידה עצום.